language: python

python:
  - 3.6

cache:
  - pip

install:
  - pip install -q pytest pytest-cov coverage
  - pip install -q chainer
  - pip install -q chainercv
  - pip install -q -e .

script:
  - pytest tests --vv
